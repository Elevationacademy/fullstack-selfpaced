# Setup

To start a new react project we will use **npm** to do a lot of the heavy lifting for us. To that end, we're going to vite (a front-end build tool), the **create vite** command, and define the name of our project (my-app in the example below).

`npm create vite@latest my-app -- --template react`